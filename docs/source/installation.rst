.. _installation:

Installing Circus
-----------------

Using EasyInstall::
    
    $ easy_install circus

Or pip::

    $ pip install circus

Trying out Circus
-----------------

To test it run the following command::

    $ cd examples && circusd circus.ini

This will launch 5 test workers.

